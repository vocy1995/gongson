<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androids.main"
    android:targetSandboxVersion="1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme" >
        <activity android:name="com.example.androids.board.BoardTab"/>
        <activity android:name="com.example.androids.person.Register"/>
        <activity android:name="com.example.androids.board.BoardListView"/>
        <activity android:name="com.example.androids.board.SingleListView"/>
        <activity android:name=".FirstActivity"/>
        <activity android:name="com.example.androids.person.FindInfo"/>
        <activity android:name=".Main"/>
        <activity android:name="com.example.androids.board.RecrSingleView"/>
        <activity android:name="com.example.androids.person.MyPage"/>
        <activity android:name="com.example.androids.board.CreateTeam"/>
        <activity android:name=".Login" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>